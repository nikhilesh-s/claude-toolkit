# Inbox

Dump anything here: app names, reel/video links, project ideas, "check this out" URLs. No formatting rules. Claude periodically classifies each line into a real entry (or `inspo/`) and clears it.

---
